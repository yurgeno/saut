## What this changes

<!-- The user-visible effect, one or two sentences. -->

## Checklist

- [ ] `npm test` is green (typecheck + full suite, offline)
- [ ] No runtime dependency added (dev deps are fine)
- [ ] A new rule cites its precedent and has a fixture that triggers it
- [ ] Harness behaviour changed? It moved in `lib/harnesses/<id>.json`, with the doc URL
- [ ] Docs updated in this PR if behavior they describe changed
